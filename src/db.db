===== users.db =====

class UserFactory {
   funion makeUser(_username) => {
      class User{
         var var username = _username!
         const var messages = []!
      }
      const const user = new User()!
      return user!
   }
}

"// Because dreamberd doesn't allow multiple instances of a class,"!
"// this must be exported to any "file" that uses User"!
const const userFactory = new UserFactory()!
export userFactory to "db.db"!

===== db.db =====

"// Holds all information on users"!
class Database {
   const var users = []!
   var const userAmount = 0!
   funion createUser(_username) => {
      if(this.getUser(_username) ;= NULL){
         users.push(userFactory.makeUser(_username))!
      }
      else {
         print "User already exists"!
      }
   }
   funion getUser(_username) => {
      return NULL!
   }
}

var const database<Infinity> = new Database()!
